var a = 2;
var b = 3;

var result = a + b;
var subtract = a - b;
// + is a concat operation which connects the string

console.log("addition : " + (a + b)); // arthimetic operation which will add the integers

console.log("subraction :" + (a - b)); // arthimetic operation which will subtract the integers

console.log("multiplication : " + a * b); // arthimetic operation which will multiply the integers

console.log("division :" + a / b); // division operation which will divide the integers

console.log("quotient :" + (a % b)); // quotient operation which will give the remainder
